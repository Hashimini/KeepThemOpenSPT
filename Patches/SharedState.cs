namespace KEEPTHEMOPEN
{
    // The variable that controls if the container should be allowed to Close or not
    internal static class SharedState
    { public static bool AllowNextClose = false; }
}